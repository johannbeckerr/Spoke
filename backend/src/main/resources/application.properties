# Base configuration shared by all profiles.
# The active profile decides which database we talk to (dev = H2, prod = PostgreSQL).
spring.profiles.active=dev

server.port=8080

# Write dates as readable ISO strings in JSON (e.g. "2026-07-10T18:30:00")
spring.jackson.serialization.write-dates-as-timestamps=false
